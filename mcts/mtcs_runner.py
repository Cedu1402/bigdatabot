import pandas as pd


def run_mcts(coin_data: pd.DataFrame, max_run_time: int, token_amount: int) -> str:
    """
    Run mcts for a coin to decide to buy, hold or sell.
    :param coin_data: OHLCV Data of coin
    :param max_run_time: Max search time
    :param token_amount: Token holding amount
    :return: Action to take (BUY, SELL) buy when already hodling coins means hold.
    """

    # get random tokens to sample from
    
    # create tree

    # run search

    return ""
